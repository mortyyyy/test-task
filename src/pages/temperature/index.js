import { Page } from "../pageLayout";

/**
 * Temperature page component
 */
export class TemperaturePage extends Page {
  constructor(root) {
    super(root, { title: "Temperature", topic: "temperature" });
  }
}
